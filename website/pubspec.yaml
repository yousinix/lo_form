name: lo_form_website
description: LoForm's website.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.13.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^2.1.0
  lo_form:
    path: ../
  sliver_tools: ^0.2.5
  url_launcher: ^6.0.9

dev_dependencies:
  lint: ^1.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
