name: example
description: Example for LoForm.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  lo_form:
    path: ../

dev_dependencies:
  lint: ^1.0.0

flutter:
  uses-material-design: true
